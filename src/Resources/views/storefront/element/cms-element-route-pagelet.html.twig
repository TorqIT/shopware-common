{% block element_route_pagelet %}

    {% set rPath = path(element.config.routeName.value) %}
    {% set rParams = element.config.routeParam.value %}

    {#  TODO - Validate and pass in params to the renderer
        Problem - The params passed into render here never makle it to the subrequest - Breakpoint the ShopwareInlinefragmentRenderer class to see.
    #}
    {% if rParams %}
        {{ render( rPath, {rParams: rParams} ) }}
    {% else %}
        {{ render( rPath ) }}
    {% endif %}
    
{% endblock %}
